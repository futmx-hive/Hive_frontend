import { Tabs, TabBtn, TabPanel, TAB_DESIGNS } from '@sharedUi/Tabs';
import UseTabs from '@hooks/UseTabs';

function FormTradeComplete() {
	return <p>this is the full trading form</p>;
}

export default FormTradeComplete;
